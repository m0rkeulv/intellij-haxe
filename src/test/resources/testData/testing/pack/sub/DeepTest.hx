package pack.sub;

// Locator fixture: utest's TeamCity reporter names this "pack_sub.DeepTest"
// (dots in the package become underscores).
class DeepTest {
  public function new() {}

  public function testDeep():Void {}
}
