package utest;

// Stub of the utest haxelib's base class; implements ITest so extends-Test
// classes resolve through the same detection root.
class Test implements ITest {
  public function new() {}
}
