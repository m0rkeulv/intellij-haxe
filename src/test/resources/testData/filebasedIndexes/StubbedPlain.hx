package com.example;

// No conditional compilation: this file IS stubable, so the stub indexes own
// it and the file-based indexes must stay empty for its names.
class StubbedPlain {
  public function new() {}
  public function plainMethod():Void {}
}
