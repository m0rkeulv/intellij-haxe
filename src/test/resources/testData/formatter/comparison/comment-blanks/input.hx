class Main {
	/*
	 * First note.
	 */
	/*
	 * Second note.
	 */
	static var counter:Int = 0;

	// leading note one
	// leading note two
	static var tally:Int = 1;

	static function main() {
		/* inline one */
		/* inline two */
		trace(counter + tally);
	}
}
