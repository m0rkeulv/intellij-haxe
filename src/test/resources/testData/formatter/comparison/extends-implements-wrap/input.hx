class ExtremelyConfigurableComponent extends BaseComponent implements Drawable implements Resizable implements Serializable implements Comparable implements Observable implements Configurable {
	public function new() {}
}

class Simple extends BaseComponent implements Drawable {
	public function new() {}
}
