# Chunk 4 — main/v2 + main/kotlin (102 files)

These packages received two dedicated review passes THIS session (the full
branch review + the checklist re-pass), with fixes applied and the suite
green after each step: LimeProjects/HxmlProjects extraction, HaxeContainers,
HaxePluginPaths, LaunchSpec, model-builder split, node interface,
ReadAction migration, structural-rule fixes. The chunk-4 sweep
(debug log overrides, wrapping constructors, mutable statics) is clean.

Open items already tracked outside this review:
- HaxeToolWindowPanel further split (optional; treeBuilder slice).
- Target stores' package move out of toolwindow (optional).
- The four deliberate v2 TODOs (mac bundle, lime-args cache, blueprint
  invalidation, processReferences) — inventoried in findings-01.

Chunk 4 complete: 102/102 (session-reviewed).
