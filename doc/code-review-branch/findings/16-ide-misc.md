# 16-ide-misc

### should-fix — src/main/java/com/intellij/plugins/haxe/ide/quickfix/typedialog/HaxeTypeCreator.java:118
The template names "Haxe Class"/"Haxe Interface"/"Haxe Enum"/"Haxe Abstract" are now spelled as bare string literals in seven touched sites across five files: the `HaxeTypeCreator.createTypePsi` switch (118-121), `HaxeIntroduceTypeInModuleQuickFix.TYPE_TEMPLATE_NAMES` (36) and its `builder.show(..., "Haxe Class", ...)` (71), `HaxeIntroduceTypeNewFileQuickFix` (70), `ExtractInterfaceHandler` (161) and `ExtractSuperClassUtil` (88) — and they must additionally stay in sync with the `<internalFileTemplate>` names in plugin.xml (356-359) and the `.ft` file names. The branch itself demonstrates the failure mode: renaming "HaxeClass" to "Haxe Class" required editing every site, and a missed one fails silently (`default -> null` in the switch, an empty filter result in the quick fix). Per the duplication rule, give the names one home — public constants (and the list) in `HaxeFileTemplateUtil`, which already owns template-name knowledge (`HAXE_TEMPLATE_PREFIX`, `getTemplateShortName`).

### should-fix — src/main/java/com/intellij/plugins/haxe/ide/inspections/HaxeUnusedFieldInspection.java:9
The switch from `ReferencesSearch`/`hasMetadata(KEEP)` to `HaxeUsageSearch` left dead imports behind in the touched files: `HaxeUnusedFieldInspection` line 9 (`HaxeMetadataCompileTimeMeta`) and line 18 (`Collection`); `HaxeUnusedFunctionInspection` line 7 (`HaxeMetadataCompileTimeMeta`), line 18 (`Collection`) and line 22 (`import static ...HaxeMeta.KEEP`); `HaxeUnusedMethodInspection` line 7 (`HaxeMetadataCompileTimeMeta`); and `HaxeGotoDeclarationHandler` line 21 (`HaxeResolver`, whose only call site now goes through `HaxeResolveChecks`). None of these symbols is referenced anywhere in their files anymore — delete the imports.

### minor — src/main/java/com/intellij/plugins/haxe/ide/intention/HaxeDefineIntention.java:66
`isAvailable` returns a hard-coded `true`, but the new `invoke` silently does nothing when `HaxeDefineContextService.activeContainerId()` is null (line 74). Under the old implementation invoke always worked (project settings always exist), so the constant `true` was honest; now the intention is offered in projects with no active container and clicking it has no effect. Gate `isAvailable` on `activeContainerId() != null` so the entry only appears when it can act.

### minor — src/main/java/com/intellij/plugins/haxe/ide/inspections/HaxeUnusedFieldInspection.java:62
The predicate `HaxeUsageSearch.usageState(decl[, scope]) != HaxeUsageSearch.UsageState.USED` — together with the identical two-line comment ("USED covers compiler-known usages too...; UNKNOWN keeps the static verdict...") — is spelled in four inspections: HaxeUnusedFieldInspection:60-64, HaxeUnusedFunctionInspection:54-58, HaxeUnusedLocalVarInspection:53-57 and HaxeUnusedMethodInspection:79-83. Same predicate in 2+ places gets one named home: add `HaxeUsageSearch.isConsideredUsed(decl, scope)` (or `isUnused`), put the comment on it once, and have the four call sites read `if (!HaxeUsageSearch.isConsideredUsed(...))`.

### minor — src/main/java/com/intellij/plugins/haxe/ide/quickfix/HaxeIntroduceTypeInModuleQuickFix.java:71
`builder.show("Unable to create haxe type", "Haxe Class", this::updateElement)` — the dialog title "Unable to create haxe type" is user-visible text inline in code, on a line this branch rewrote (same at HaxeIntroduceTypeNewFileQuickFix.java:70). User-visible strings belong in a bundle: add a key to HaxeBundle.properties and use `HaxeBundle.message(...)` at both call sites.

### minor — src/main/java/com/intellij/plugins/haxe/ide/generation/BaseHaxeGenerateAction.java:49
The changed hunk sits directly under the lingering `//TODO getData for PSI_FILE does not seem to work anymore?` and adds another layer of workaround (null-checking `editor.getVirtualFile()` for console/preview editors). Per the TODO rule, a TODO in changed code is either solved, solved now, or states WHAT is deferred — a trailing question mark states neither. Verify whether `CommonDataKeys.PSI_FILE` works on current platform builds (and drop the fallback if it does), or reword the TODO to say what is actually deferred.
