# 09-lang-psi-indexes

### question — src/main/java/com/intellij/plugins/haxe/lang/psi/indexes/unified/HaxeClassNameUnifiedIndex.java:116-121
The new `isActiveTargetPackage` post-filter in `getByNameFiltered` applies to every caller, and there are many beyond completion: `HaxeGotoClassContributor`, `HaxeGotoClassSymbolContributor`, `TreeHaxeClassChooserDialog`, `HashLinkSourceResolver`, `HaxeVariableSourceNavigator`, `HaxeUnresolvedTypeAnnotator`, `HaxeReferenceCopyPasteProcessor`, `HaxeTestFinder`, `HaxeDocumentationCodeVisitor`. The comment justifies it only for "import candidates", but the filter now also hides e.g. `js.html.*` classes from goto-class, the class chooser and the debugger source resolvers whenever `HaxeDefineDetectionManager.getAllDefinitions()` lacks the root define — including the cold-start case where define detection has not populated yet, which would blank out ALL target-root classes from navigation. Is this intended for navigation/debugger callers, or should the filter live only in the completion/import paths (as `getCompletionData` already does per-leg)?

### should-fix — src/main/java/com/intellij/plugins/haxe/lang/psi/indexes/filebased/data/HaxeComponentIndexData.java:26
The new `visibilityInherited` flag is written by the indexer (`HaxeIndexDataUtil.createIndexData(HaxeMethodModel)`) and serialized by `HaxeComponentExternalizer`, but no query-side code ever reads `isVisibilityInherited()` — the only references are the data class, the externalizer and the setter call. Per the checklist, consumer-less API is deleted or carries a `TODO` naming the wiring it waits for; the field javadoc describes what consumers *should* do but nothing does it. Either wire the intended consumer (the parent-chain resolve at query time) or add a `TODO:` naming it; otherwise the byte is paid in every index entry for nothing.

### should-fix — src/main/java/com/intellij/plugins/haxe/lang/psi/indexes/filebased/indexer/HaxeClassInheritanceIndexer.java:22-26
Identical class javadoc pasted into both this file and `HaxeTypedefInheritanceIndexer.java:23-27`, and it violates the comment rules: "we are not allowed", "We solve the problem", "how we  resolved FQN" are authorship/history narration; "fie" should be "file", "its" should be "it's". Rewrite once as behaviour: keys are simple names because an indexer must not read outside the file being indexed (resolving an FQN would), and consumers filter false positives (same simple name, different package) at query time — `HaxeInheritanceDefinitionsSearcher.directlyInheritsFrom` already states this on the consumer side.

### should-fix — src/main/java/com/intellij/plugins/haxe/lang/psi/indexes/filebased/extension/fqn/HaxeFullyQualifiedMemberNameIndex.java:109-117
The new module-level-member branch is a verbatim copy of the existing `else` block at lines 89-96 (`moduleModel.getMember(fqn.memberName, null)` + `findAndAddMember` + the `HaxeMethodModel`/`findAndAddParameter` dance), making the fourth occurrence of that three-step sequence in one method. Extract one helper, e.g. `boolean addMemberOrParameter(HaxeBaseMemberModel member, FullyQualifiedInfo fqn, List<PsiElement> results)`, and each branch collapses to a member lookup plus one call — the checklist's "same expression spelled in 2+ places gets one named home".

### minor — src/main/java/com/intellij/plugins/haxe/lang/psi/stubs/serializers/HaxeClassStubSerializer.java:60-73
The typedef and class branches are two loops identical except for the stub index key, and both were edited in this branch to add `getSimpleName`. Per "the same expression evaluated in several branches becomes one local": name the key first (`StubIndexKey<String, HaxeClass> key = stub.getPsi().isTypeDef() ? HaxeTypedefInheritanceStubIndex.KEY : HaxeClassInheritanceStubIndex.KEY;`) and keep a single loop over `stub.getSuperTypeNames()`.

### minor — src/main/java/com/intellij/plugins/haxe/lang/psi/indexes/filebased/indexer/HaxeFullyQualifiedNameIndexer.java:21-31
The new field/constructor use 2-space indentation against the class's 4-space body, the `switch` block at lines 50-57 drifts a further level, `enum CollectType{` and `switch (collectType){` lack the space before the brace, two stray blank lines remain at 44-45 where the old `indexDataMap` declaration was deleted, and the file gained a trailing blank line after the closing brace. Also, the nested enum sits between the constructor and `map()`; nested types read best last. Pure formatting, but this is a fully rewritten region so it should match the file.

### minor — src/main/java/com/intellij/plugins/haxe/lang/psi/indexes/unified/HaxeClassNameUnifiedIndex.java:60-65
The reworked completion pipeline still maps with a lambda that nests another lambda: `.map(indexData -> new HaxeClassLookupData(indexData, () -> { return resolveModel(indexData.getFqn(), project, scope); }))`. Per "a lambda that NESTS another lambda becomes a private method", extract e.g. `private static HaxeClassLookupData lazyLookupData(HaxeComponentIndexData indexData, Project project, GlobalSearchScope scope)` and map with a bound call; at minimum the inner `() -> { return ...; }` should be the expression form.

### minor — src/main/java/com/intellij/plugins/haxe/lang/psi/indexes/unified/fqn/HaxeFullyQualifiedClassNameUnifiedIndex.java:37-43
`getAllKeys` reaches past the compiler leg's facade and calls `HaxeCompilerTypeCatalogService.getInstance(project).allFqns()` directly, while the sibling `HaxeClassNameUnifiedIndex` goes through `HaxeClassNameCompilerIndex.getAllKeys(project)`. Give `HaxeFullyQualifiedClassNameCompilerIndex` a `getAllKeys` and call that, keeping the catalog service behind the compiler-index leg on both paths (and dropping the service import from the unified class).

### minor — src/main/java/com/intellij/plugins/haxe/lang/psi/indexes/filebased/extension/HaxeClassFieldNameFileIndex.java:69
The seven `return false` → `return true` fixes carry three different comment texts for the same fact: the full form ("keep iterating; false stops at the FIRST file and drops every other candidate") in five files, a truncated `// keep iterating;` here, and `// true = keep iterating` in `HaxeClassNameFileIndex.java:76`. Use the full form everywhere — it is the only variant that states the reason.

### minor — src/main/java/com/intellij/plugins/haxe/lang/psi/indexes/filebased/extension/HaxeClassNameFileIndex.java:79-82
Deleting the commented-out lookup block left three consecutive blank lines between the `getFilesWithKey` call and `return classes;`. Collapse to one — blank lines separate groups, and this scar advertises the deletion.

### minor — src/main/java/com/intellij/plugins/haxe/lang/psi/indexes/filebased/extension/fqn/HaxeFqnIndexUtil.java:25
After this branch removed the dead `data`/`className` lines from the processor, nothing in `resolveModule` uses the `fqn` parameter (it was already unread before, but the method body is squarely in the changed code now). Drop the parameter and update the three callers in `HaxeFullyQualifiedMemberNameIndex`, `HaxeFullyQualifiedModuleNameIndex` and `HaxeFullyQualifiedClassNameIndex`.
